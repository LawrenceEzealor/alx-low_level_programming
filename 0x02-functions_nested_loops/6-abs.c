#include "main.h"
/**
 * _abs - computes the abs value of an integer
 * @n: the integer in question
 * Return: the abs(absolute value) of the integer
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
