#include <stdio.h>
/**
 * main - this is a program that prints a line with puts function
 * Return: 0 (when this return zero, this project is a success)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
